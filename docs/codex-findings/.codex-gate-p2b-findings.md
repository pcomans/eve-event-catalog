[HIGH] catalog/providers/gap-replay.ts:54-63 — mergeGapTrades concatenates historical then buffered rather than merging chronologically. A live-only trade inside the REST overlap is replayed after later historical trades, which can create or miss an edge and leave finalPrevious/cursor stale; tests cover only a suffix overlap where concatenation happens to work.
[HIGH] catalog/providers/gap-replay.ts:106-108,126-139 — no cursor-boundary filtering or comparison exists. Alpaca historical start is inclusive, so the cursor trade and earlier trades sharing its timestamp can be replayed from the persisted lastPrice, causing false crossings; advanceCursor also accepts an older/equal ambiguous last row and can regress the cursor.
[HIGH] catalog/providers/gap-replay-cursor.ts:34-35 — writeCursor is an unconditional SET with neither a fencing token nor an atomic cursor comparison. A delayed session can overwrite a newer persisted cursor, so the adapter provides no stale-writer or regression protection.
[HIGH] catalog/providers/fence-redis.ts:32-34 — isFencedWriteAllowed performs only GET then returns a boolean; the protected write/delivery occurs later. A holder can pass the check, another holder can INCR, and the stale holder can still commit, so the adapter has the exact read-check-act gap fencing must close. Enforce token validation atomically with each protected mutation or at the downstream write boundary.
[HIGH] catalog/providers/edgar-redis.ts:20-29 — SMEMBERS followed later by SADD does not make overlapping sweeps safe: two workers can both read an accession as unseen, both deliver it, then both idempotently add it. SADD dedupes storage, not delivery; claiming/deduping must be atomic with defined failure recovery.
[MEDIUM] catalog/providers/desired-membership.ts:8-17 — delivering should not count as desired membership: its predicate is already claimed and retries use the stored deliverReason/snapshot; other armed subscriptions retain the resource independently, and a re-arm creates a new armed row. Including stranded delivering rows can retain symbol/order watches indefinitely and cause redundant source-triggered attempts.
[HIGH] catalog/providers/alpaca-client.ts:195-196 — Alpaca after/until filters are exclusive and apply to order submitted_at, not the terminal transition. An order submitted before after but filled/canceled during the socket gap is omitted, exact-boundary rows are excluded, and a local-clock cursor can miss rows under skew; this cannot implement the documented [after, until] terminal-state recovery.
[MEDIUM] catalog/providers/alpaca-client.ts:195-196 — getAllOrders returns one page with a default limit of 50 and this SDK does not auto-page that endpoint. A watched closed order outside the first page is silently missed; use explicit pagination/high-water logic or query watched IDs directly.
[MEDIUM] catalog/providers/order-reconciliation.ts:52-61 — reconcileClosedOrders never adds an emitted orderId to its local delivered set, so duplicate rows for one order in a single fetch produce duplicate WakeDecisions. The overlap test only seeds a later call with the first result and does not exercise same-response dedupe.
[MEDIUM] catalog/providers/alpaca-client.ts:197-201 — status=closed is blindly cast to the four-value OrderTerminalStatus even though Alpaca can return other closed lifecycle values such as replaced. Runtime data can therefore escape as an invalid wake status; explicitly filter/map the intended terminal statuses.
[MEDIUM test] catalog/providers/fence-redis.test.ts:20-27 — the real-Redis monotonicity test awaits three INCR calls sequentially, so it never verifies uniqueness/strict monotonicity under concurrent acquisitions as claimed by the gate. Exercise simultaneous acquireFenceToken calls and assert the returned token set and final counter.
[MEDIUM test] catalog/providers/edgar-redis.test.ts:23-33,50-66 — the advertised overlap/end-to-end cases execute reads, diffs, and adds sequentially. They cannot expose the production read-diff-deliver-SADD race and therefore pass while two overlapping workers can double-deliver.
[MEDIUM test] catalog/providers/desired-membership.test.ts:14-16,71-86 — listSubscriptions reads the global registry, but the tests use fixed common symbols and make symbol-level presence/absence/count assertions. Concurrent tests or real rows can create false failures/interference, and cleanup is registered only after all creates, so a mid-setup failure leaks earlier rows; use unique resources and register cleanup immediately.
[MEDIUM test] catalog/providers/order-reconciliation.test.ts:77-100 — the test named overlapping sweeps runs them sequentially and manually passes the first result as alreadyDelivered. Two real concurrent sweeps can both start from the same empty snapshot and both decide to wake, so this assertion is vacuous for overlap safety.
[MEDIUM test] catalog/providers/alpaca-client.ts:173-202 — neither new REST wrapper has a direct test. The suite therefore never verifies inclusive historical cursor handling, SDK page-token exhaustion/repeated-token termination, 429 behavior, closed-order limits, or the actual exclusive submission-time meaning of after/until.
[HIGH] catalog/providers/alpaca-client.ts:183-187 — the SDK exposes timestampRaw with nanosecond precision, but the adapter stores timestamp.toISOString(), truncating every cursor/dedupe timestamp to milliseconds. Multiple prints in one millisecond become cursor-equal, so an inclusive reconnect cannot reliably distinguish which same-time rows precede or follow the persisted trade.
VERDICT: FAIL
