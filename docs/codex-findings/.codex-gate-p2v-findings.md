[HIGH] agent/channels/catalog.ts:22-24, catalog/wake.ts:779, connector/lib/alpaca-session.ts:577-584 — The eve app still unconditionally arms its in-process Alpaca provider while the connector independently opens the same account streams; no host-mode switch/extraction exists. This violates the one-market-data-connection constraint, and when the connector wins delivery its direct fired update never disarms the eve-side watch.
[HIGH] connector/lib/alpaca-session.ts:235-237, catalog/providers/gap-replay.ts:56-59 — Historical trades now retain timestampRaw nanoseconds, but buffered/live trades still use Date.toISOString() milliseconds. The mixed-width strings neither dedupe nor sort correctly: e.g. a genuinely later .123999999Z historical trade compares before a persisted .123Z live cursor and is filtered out.
[HIGH] connector/lib/alpaca-session.ts:593-599,418-435 — seedingSymbols is set before subscribeForTrades, but the code never awaits the stock-stream subscription acknowledgement before fixing the historical REST end boundary. A trade between that REST boundary and Alpaca accepting the subscription is in neither history nor the live buffer.
[HIGH] connector/lib/alpaca-session.ts:299-344 — The second buffer swap happens before replay/delivery/cursor I/O; trades arriving during that awaited work accumulate in the new buffer, then seedingSymbols is cleared without draining them. Those trades are not processed until a later session replay, so a crossing can be delayed for the rest of the 10-minute step.
[HIGH] connector/lib/alpaca-session.ts:378-389 — The prior reconnect verdict remains unresolved: SDK auto-reconnect callbacks only log. A mid-step reconnect restores subscriptions without putting symbols back into seeding mode or replaying the socket gap, so a cross-and-recovery is missed until the next workflow step.
[HIGH] connector/lib/alpaca-session.ts:423-435 — The prior new-subscription-on-an-existing-symbol gap is not closed: adopting a live sibling previous value skips every trade between the new subscription armedAt and the next membership poll. A crossing that fires and recovers in that interval is missed, and the next session can instead replay trades from before this subscription was armed.
[HIGH] connector/routes/ensure-running.get.ts:23-30 — Heartbeat read plus start is not protected by a Redis claim/lock. Vercel Cron can deliver the same event concurrently, so two stale checks can both start root runs; their distinct run nonces make claimChain irrelevant and both forever-chains survive, continually fencing each other.
[MEDIUM] connector/lib/alpaca-session.ts:616-630 — pendingWrites is snapshotted by Promise.allSettled while both streams are still connected. A callback can append a new delivery during the drain; it is outside that snapshot and is then abandoned when disconnect/step return occurs.
[MEDIUM] catalog/providers/alpaca-client.ts:214-227, connector/lib/alpaca-session.ts:610-615 — Reconciliation queues one REST GET per watched order on every 15-second cadence with no batch/concurrency/deadline bound. The SDK limiter only throttles the queue; sufficiently many armed orders stretch the fixed 40-iteration step past maxDuration:800, causing repeated hard kills rather than a bounded session.
[MEDIUM] connector/lib/alpaca-session.ts:336-339,347-375 — Cursor persistence occurs only after initial seeding; ordinary live trades update previous in memory but never advance the Redis cursor. This fails AT-11s after-any-processed-trade criterion and forces every reconnect to replay the entire preceding session rather than resume from its last processed trade.
[HIGH] catalog/providers/alpaca-client.ts:171-182, connector/lib/alpaca-session.ts:281-315 — First-connect replay has no valid boundary/baseline: it fetches an arbitrary five minutes of history, including trades before subscription armedAt, then uses the current latest price as the previous value for that older sequence. This can manufacture a wake for a crossing that happened before the subscription existed.
VERDICT: FAIL
