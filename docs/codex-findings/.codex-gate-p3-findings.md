- P1 connector/workflows/{edgar,expiry,recovery}-sweep.ts:27,23,24 — 500 ticks consume ~2,506 workflow events per run (500 × [3-event step + 2-event sleep] + nonce/chaining steps), exceeding the documented requirement to chain before ~2,000 events; cap these loops below 400 ticks.
- P1 catalog/providers/edgar-sweep.ts:111 — The CIK seen-set is advanced before any deliver() call. A crash after ZADD (or a first delivery CAS/Redis failure) makes the filing non-fresh forever while affected subscriptions remain armed, violating the required persist-delivery-decision-before-cursor-advance ordering.
- P1 catalog/providers/edgar-sweep.ts:108-122 — Fresh filings are delivered to every current subscriber using only the CIK-wide baseline/earliest armedAt. A subscriber armed after a filing was accepted but before the next sweep (or after an idle gap with an existing seen-set) is incorrectly woken for that pre-subscription filing; filter each delivery by filing.acceptanceDateTime > sub.armedAt.
- P1 catalog/wake.test.ts:272-294 — This firedAt assertion is full-suite flaky because concurrent recovery-sweep tests scan the same Redis registry while their per-process fetch stubs do not share the production Redis delivery marker: both POSTs report success and the recovery writer can store a later firedAt. Serialize these live-Redis files or make the fake marker cross-process; the normal production marker paths preserve the original firedAt.
- P1 connector/lib/deliver-wake.ts:115-163 — CAS-miss resumes correctly reuse the persisted reason/snapshot, but a wake claim that expires after send() succeeds and before the route upgrades it to sent allows recovery to claim and POST again. The second attempt mints a new firedAt, so the terminal row can be later than the first envelope the agent received; this is a real production dual-write window, not just the shared-Redis test flake.
- P1 connector/workflows/{edgar,expiry,recovery}-sweep.ts:32,29,30 — Heartbeats are stale after 5 minutes and are written only before a tick, while connector steps may remain live for the configured 800-second function duration. A slow tick can be restarted by the */5 cron; claimSupervisorLock only serializes supervisors and per-run claimChain cannot stop the old run resuming, so two forever chains then persist. Use a stale window beyond the maximum live/retry interval or fence root-chain ownership.
- P1 connector/lib/deliver-wake.ts:163-164 — The terminal update result is discarded and recordEvent receives the stale caller sub, so connector-fired history rows have action fired/expired but status armed (or delivering on recovery). Record from the updated terminal subscription returned by updateSubscription.
VERDICT: FAIL
