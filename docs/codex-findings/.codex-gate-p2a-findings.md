- P0 connector/lib/alpaca-session.ts:68 — Connector wakes bypass `deliverWake`'s armed→delivering→terminal lifecycle: the route POST plus `recordEvent` never updates or disarms the subscription, so it remains armed while the subscriptionId marker suppresses later wakes and history records an armed row as fired.
- P0 connector/lib/alpaca-session.ts:93 — Non-2xx wake responses are logged and converted to success, and an `alreadyInFlight` 200 is not inspected; `replayGapForSymbol` then advances the cursor at line 175, so an unperformed wake is no longer replayable and the delivering recovery sweep cannot see it.
- P0 connector/lib/alpaca-session.ts:100 — `guardedWrite` performs a Redis GET/check followed by an unrelated HTTP/Redis commit; the token is not carried into `/catalog/wake`, `recordEvent`, or `writeCursor`, so a newer INCR between check and commit still permits the stale zombie write.
- P1 connector/lib/alpaca-session.ts:306 — Stock and trading streams mint tokens from independent counters, but the trading token is discarded and the stock token guards both streams. Those counters/parallel INCR results need not match, so the current trading holder can be fenced out (or checked against the wrong ownership generation).
- P0 connector/lib/alpaca-session.ts:148 — Gap replay runs before the stock socket exists and always merges `historical` with an empty buffer; SDK auto-reconnects only log at line 212. Trades in the REST-end→authentication seam are not buffered, and a later socket drop neither gracefully ends the step nor performs replay before subscriptions resume.
- P1 connector/lib/alpaca-session.ts:348 — Each cadence reconciles only `newlyWatched` orders but advances the account-wide `lastReconcileAt` anyway. A watched order whose websocket terminal update was missed before that tick then falls before the next session's `after` bound and is never REST-recovered.
- P1 connector/lib/alpaca-session.ts:339 — Every 15s membership read rebuilds all `PriceWatch` objects with `previous: null`; unchanged watches therefore skip crossing evaluation on the first subsequent trade. A new subscription for an already-streamed symbol also gets no replay because the symbol delta is empty.
- P1 connector/nitro.config.ts:11 — Nothing configures Vercel `maxDuration: 800`, so the 720s step has not actually requested the Fluid ceiling it assumes. Moreover, connector/lib/alpaca-session.ts:327 budgets 720s of sleeps alone; initial replay/connect plus all 48 cadence bodies consume the nominal 80s buffer and can exceed 800s.
- P1 connector/lib/alpaca-session.ts:192 — Live price and order deliveries are launched with `void` and never tracked. The normal handoff disconnects and returns at line 357 without draining them, so a late event's through-write can be frozen/killed after the step response or fenced out by the next step.
- P1 connector/lib/alpaca-session.ts:323 — Both `connect()` calls are fire-and-forget and authentication is never awaited; genuine initial auth/connect failure only logs/disconnects while the workflow step continues sleeping for the full session instead of throwing for a retry.
- P1 connector/lib/alpaca-session.ts:357 — Socket cleanup exists only on the success path. Any Redis/REST/membership exception after connection rejects the step without disconnecting either stream, so the workflow retry can open replacement account sockets while the failed invocation's sockets remain live.
- P1 connector/lib/alpaca-session.ts:151 — `ALPACA_DATA_FEED=test` cannot bootstrap in the connector: before opening its first FAKEPACA stream it asks `getLatestTrade(test)`, whose process-local cache can only be populated by a stream tick; in this separate service that tick has not and cannot occur yet.
VERDICT: FAIL
